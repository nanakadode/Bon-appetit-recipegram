<h1>New Recipe</h1>
<%= simple_form_for @recipe do |f| %>
  <%= f.input :title, label: 'Title' %>
  <%= f.input :body,  label: 'Discription', { row: 5 } %>
  <%= f.input :image, as: :file, input_html { multiple: true } %>
  <%= f.button :submit %>
<% end %>
